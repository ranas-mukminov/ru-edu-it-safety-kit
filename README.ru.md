# 🛡️ ru-edu-it-safety-kit

![CI](https://github.com/ranas-mukminov/ru-edu-it-safety-kit/actions/workflows/ci.yml/badge.svg)
![License](https://img.shields.io/badge/license-Apache--2.0-blue.svg)
![Python](https://img.shields.io/badge/python-3.9%2B-blue.svg)

🇷🇺 Русская версия | 🇬🇧 [English version](README.md)

Базовый набор инструментов для обеспечения информационной безопасности в российских школах и колледжах. Этот инструментарий предоставляет комплексный набор средств и шаблонов конфигураций для создания защищённой, соответствующей нормативам и легко поддерживаемой ИТ-инфраструктуры в образовательных учреждениях.

**Автор:** [Ранас Мукминов](https://github.com/ranas-mukminov)  
**Сайт:** [run-as-daemon.ru](https://run-as-daemon.ru)

## Описание

`ru-edu-it-safety-kit` — это open-source проект, разработанный специально для ИТ-администраторов и технического персонала российских образовательных учреждений (школ, колледжей, техникумов). Он решает уникальные задачи защиты образовательных сетей: безопасность учащихся, фильтрация контента, сегментация сети и соответствие российским нормативным требованиям.

Набор предоставляет декларативную конфигурацию, автоматизированное планирование мер безопасности и готовые шаблоны документации. Он помогает школам внедрить базовые меры защиты без необходимости глубокой экспертизы в сетевых технологиях или дорогостоящих консультантов.

## Основные возможности

- **Декларативные профили школы:** Описание сетевой топологии (провайдеры, здания, VLAN, роли) в простом формате YAML
- **Автоматизированное планирование безопасности:** Генерация технических и организационных мер защиты на основе профиля инфраструктуры
- **Генератор образовательного контента:** Создание готовых к печати листовок и руководств для родителей, учащихся и учителей на русском языке
- **Поддержка разных вендоров:** Шаблоны конфигураций для OpenWrt, pfSense и MikroTik RouterOS
- **Сегментация сети:** Изоляция на основе VLAN для административных, учительских, ученических и гостевых сетей
- **Фильтрация контента:** Конфигурации для DNS-фильтрации (совместимо с SkyDNS, Yandex.DNS)
- **Централизованное логирование:** Docker-примеры для агрегации логов (стек Loki/Grafana)
- **Ориентация на соответствие:** Учитывает российские образовательные регламенты и требования информационной безопасности
- **Готовность к продакшену:** Включает CI/CD, тесты, линтеры и проверки безопасности

## Архитектура и компоненты

Набор состоит из трёх основных уровней:

```
┌─────────────────────────────────────────────────────────┐
│            CLI-инструмент (ru-safety)                    │
│                  Python 3.9+ / Click                     │
└─────────────────────────────────────────────────────────┘
                           │
           ┌───────────────┼───────────────┐
           ▼               ▼               ▼
    ┌──────────┐    ┌───────────┐   ┌──────────────┐
    │Валидатор │    │ Планиров- │   │  Генератор   │
    │          │    │щик мер    │   │  листовок    │
    └──────────┘    └───────────┘   └──────────────┘
           │               │               │
           └───────────────┴───────────────┘
                           ▼
              ┌─────────────────────────┐
              │   Профиль школы YAML    │
              │ (топология, VLAN, ISP)  │
              └─────────────────────────┘
                           │
           ┌───────────────┼───────────────┐
           ▼               ▼               ▼
    ┌──────────┐    ┌───────────┐   ┌──────────────┐
    │ Шаблоны  │    │  Шаблоны  │   │   Шаблоны    │
    │ OpenWrt  │    │  pfSense  │   │  RouterOS    │
    └──────────┘    └───────────┘   └──────────────┘
```

**Компоненты:**

1. **Python CLI (`ru-safety`)**: Основной инструмент для валидации, планирования и генерации контента
2. **Модели Pydantic**: Типо-безопасные определения профилей с валидацией
3. **YAML-профили**: Декларативное описание инфраструктуры
4. **Шаблоны Jinja2**: Генерация Markdown-листовок для заинтересованных лиц
5. **Шаблоны конфигураций**: Статические сниппеты для сетевых устройств (подход copy-paste-adapt)
6. **Docker-стек логирования**: Пример настройки для централизованного сбора логов

## Требования

### Системные требования

- **Операционная система:** Linux (Ubuntu 20.04+, Debian 11+, RHEL 8+, Rocky Linux 8+) или macOS
- **Python:** 3.9 или выше
- **Дисковое пространство:** ~100 МБ для инструментария и зависимостей
- **Память:** минимум 512 МБ RAM
- **Сеть:** Доступ в Интернет для установки зависимостей

### Требования к доступу

- Обычная учётная запись пользователя (root не требуется для запуска CLI)
- Доступ `sudo` для развёртывания конфигураций на сетевых устройствах

### Опциональные зависимости

- **Docker и Docker Compose:** Для запуска стека централизованного логирования
- **Сетевые устройства:** OpenWrt, pfSense или MikroTik RouterOS для применения конфигураций
- **Генератор PDF:** Pandoc или аналогичный инструмент для конвертации Markdown-листовок в PDF

## Быстрый старт

Начните работу за 5 минут:

```bash
# 1. Клонируйте репозиторий
git clone https://github.com/ranas-mukminov/ru-edu-it-safety-kit.git
cd ru-edu-it-safety-kit

# 2. Установите инструментарий
python3 -m venv .venv
source .venv/bin/activate
pip install -e .

# 3. Проверьте пример профиля школы
ru-safety validate configs/topologies/single_isp_school.yml

# 4. Сгенерируйте план безопасности
ru-safety plan configs/topologies/single_isp_school.yml

# 5. Сгенерируйте образовательные листовки
ru-safety generate-leaflets configs/topologies/single_isp_school.yml --output-dir ./output
```

**Следующие шаги:**

- Создайте свой профиль школы: `cp configs/topologies/single_isp_school.yml my-school.yml`
- Отредактируйте `my-school.yml` с данными вашей сети
- Примените сгенерированные конфигурации к вашим сетевым устройствам
- Распечатайте и распространите листовки среди родителей, учащихся и учителей

## Подробная установка

### Установка на Ubuntu / Debian

```bash
# Установите Python 3.9+ и venv
sudo apt update
sudo apt install -y python3 python3-venv python3-pip git

# Клонируйте репозиторий
git clone https://github.com/ranas-mukminov/ru-edu-it-safety-kit.git
cd ru-edu-it-safety-kit

# Создайте виртуальное окружение
python3 -m venv .venv
source .venv/bin/activate

# Установите инструментарий
pip install -e .
```

### Установка на RHEL / Rocky / AlmaLinux

```bash
# Установите Python 3.9+ и git
sudo dnf install -y python39 python39-pip git

# Клонируйте репозиторий
git clone https://github.com/ranas-mukminov/ru-edu-it-safety-kit.git
cd ru-edu-it-safety-kit

# Создайте виртуальное окружение
python3.9 -m venv .venv
source .venv/bin/activate

# Установите инструментарий
pip install -e .
```

### Установка для разработки

Для контрибьюторов и разработчиков:

```bash
# Установите с зависимостями для разработки
pip install -e .[dev]

# Запустите тесты
pytest

# Запустите линтеры
ruff check .
black --check .

# Запустите проверки безопасности
bandit -r src/
pip-audit
```

## Настройка

### Создание профиля школы

Создайте YAML-файл, описывающий вашу сетевую топологию:

```yaml
# my-school.yml
name: "МБОУ СОШ №1"
type: school

isps:
  - name: "Ростелеком"
    speed_mbps: 100
    is_primary: true

buildings:
  - name: "Главный корпус"
    is_hq: true
    vlans:
      - id: 10
        name: "admin"
        role: admin
        subnet: "10.10.10.0/24"
      - id: 20
        name: "teachers"
        role: teacher
        subnet: "10.10.20.0/24"
      - id: 30
        name: "students"
        role: student
        subnet: "10.10.30.0/24"
      - id: 40
        name: "guests"
        role: guest
        subnet: "10.10.40.0/24"
```

**Поля профиля:**

- `name`: Название школы (любая строка)
- `type`: `school` или `college`
- `isps`: Список интернет-провайдеров
  - `name`: Название провайдера
  - `speed_mbps`: Скорость соединения в Мбит/с
  - `is_primary`: Флаг основного канала (булево значение)
- `buildings`: Список зданий
  - `name`: Название здания
  - `is_hq`: Флаг главного здания
  - `vlans`: Сетевые сегменты
    - `id`: ID VLAN (1-4094)
    - `name`: Название VLAN
    - `role`: `admin`, `teacher`, `student` или `guest`
    - `subnet`: IP-подсеть в нотации CIDR

### Примеры профилей

Инструментарий включает два эталонных профиля:

- **`configs/topologies/single_isp_school.yml`**: Небольшая школа, один провайдер, одно здание
- **`configs/topologies/multi_building_college.yml`**: Колледж с несколькими зданиями и VPN-связью

### Конфигурации сетевых устройств

Шаблоны конфигураций находятся в каталоге `configs/`:

- **`configs/openwrt/`**: Конфигурации VLAN и межсетевого экрана для OpenWrt
- **`configs/pfsense/`**: Правила VLAN и фильтрации для pfSense
- **`configs/routeros/`**: Скрипты интерфейсов и межсетевого экрана для MikroTik RouterOS
- **`configs/logging/`**: Docker Compose стек для логирования Loki/Grafana

Эти шаблоны **не полностью автоматизированы** — они служат справочным материалом для копирования и адаптации. Вы должны применять их вручную к вашим устройствам.

## Использование и типичные задачи

### Валидация профиля школы

Проверьте, корректен ли ваш YAML-профиль синтаксически и логически:

```bash
ru-safety validate my-school.yml
```

Вывод:
```
✅ Profile 'МБОУ СОШ №1' is valid.
⚠️  Warning: No backup ISP configured - consider redundancy.
```

### Генерация плана безопасности

Создайте контрольный список рекомендуемых мер безопасности:

```bash
ru-safety plan my-school.yml
```

Вывод:
```
🛡️  Safety Plan for: МБОУ СОШ №1

Technical Measures:
 - Configure VLAN segmentation (4 VLANs detected)
 - Enable DNS-based content filtering for student network
 - Configure firewall rules to isolate student VLAN from admin/teacher networks
 - Enable DHCP snooping on switches
 - Configure centralized logging

Organizational Measures:
 - Create acceptable use policy for students
 - Train teachers on network security basics
 - Establish incident response procedure
 - Document network topology and configurations
```

### Генерация образовательных листовок

Создайте Markdown-документы для заинтересованных лиц:

```bash
ru-safety generate-leaflets my-school.yml --output-dir ./leaflets
```

Выходные файлы:
- `leaflets/parents.md`: Правила ИТ-безопасности для родителей
- `leaflets/students.md`: Правила ИТ-безопасности для учащихся
- `leaflets/teachers.md`: Руководство по ИТ-безопасности для учителей

Конвертация в PDF с помощью Pandoc:

```bash
pandoc leaflets/parents.md -o leaflets/parents.pdf --pdf-engine=xelatex -V mainfont="DejaVu Sans"
```

### Применение сетевых конфигураций

1. Изучите соответствующий шаблон:
   - Для OpenWrt: `configs/openwrt/vlan_config.conf`
   - Для pfSense: `configs/pfsense/firewall_rules.xml`
   - Для RouterOS: `configs/routeros/firewall.rsc`

2. Адаптируйте шаблон под вашу топологию (замените IP-адреса, ID VLAN)

3. Примените вручную через веб-интерфейс устройства или CLI

4. Проверьте связность для каждого VLAN

### Запуск централизованного логирования (опционально)

```bash
cd configs/logging/loki-stack
docker-compose up -d
```

Откройте Grafana по адресу `http://<IP_ВАШЕГО_СЕРВЕРА>:3000` (учётные данные по умолчанию: `admin/admin`)

Настройте ваши сетевые устройства для отправки логов на конечную точку Loki.

## Обновление

### Обновление инструментария

```bash
cd ru-edu-it-safety-kit
git pull origin main

# Переустановите зависимости
source .venv/bin/activate
pip install -e .
```

### Миграция профилей

Если схема профиля изменится, инструкции по миграции будут предоставлены в файле `CHANGELOG.md`. Всегда проверяйте ваши профили после обновления:

```bash
ru-safety validate my-school.yml
```

## Логи, мониторинг и устранение неполадок

### Проверка логов CLI

CLI выводит данные напрямую в stdout/stderr. Используйте перенаправление оболочки для логирования:

```bash
ru-safety plan my-school.yml 2>&1 | tee plan-output.log
```

### Типичные проблемы

**Проблема:** `ru-safety: command not found`

**Решение:** Активируйте виртуальное окружение:
```bash
source .venv/bin/activate
```

**Проблема:** Валидация YAML не проходит с ошибкой «invalid schema»

**Решение:** Проверьте опечатки в именах полей. Сравните с примерами в `configs/topologies/`. Убедитесь, что ID VLAN — целые числа, а не строки.

**Проблема:** Нет данных в дашбордах Grafana

**Решение:**
- Проверьте Docker-контейнеры: `docker-compose ps`
- Проверьте конфигурацию отправки логов на сетевых устройствах
- Проверьте логи Loki: `docker-compose logs loki`

**Проблема:** Отказано в доступе при выполнении скриптов

**Решение:** Убедитесь, что скрипты исполняемые:
```bash
chmod +x scripts/*.sh
```

### Устранение неполадок сетевых устройств

**OpenWrt:**
```bash
# Проверьте конфигурацию VLAN
cat /etc/config/network

# Просмотрите правила межсетевого экрана
iptables -L -v -n
```

**pfSense:**
- Перейдите в Diagnostics → States → Show States
- Проверьте журналы межсетевого экрана в Status → System Logs → Firewall

**MikroTik RouterOS:**
```bash
/interface vlan print
/ip firewall filter print
```

## Замечания по безопасности

### Базовый контрольный список безопасности

- **Смените пароли по умолчанию:** Обновите стандартные учётные данные на всех сетевых устройствах
- **Включите HTTPS:** Используйте TLS для доступа к веб-интерфейсам маршрутизаторов и коммутаторов
- **Ограничьте доступ к управлению:** Ограничьте административный VLAN конкретными IP-диапазонами
- **DNS-фильтрация:** Принудительно направляйте учащихся через фильтрующий DNS (например, SkyDNS, Yandex.DNS)
- **Регулярные обновления:** Поддерживайте прошивку сетевых устройств и зависимости инструментария актуальными
- **Журналы доступа:** Включите и регулярно просматривайте логи подключений на наличие аномалий
- **Физическая безопасность:** Защитите сетевое оборудование в запираемых шкафах
- **Правила межсетевого экрана:** Блокируйте межVLAN-трафик за исключением необходимых сервисов (например, DNS, NTP)

### Фильтрация контента

Этот инструментарий **не включает средства обхода блокировок**. Он предоставляет конфигурации для легальных решений фильтрации контента, соответствующих российскому законодательству.

Рекомендуемые провайдеры DNS-фильтрации для школ:
- SkyDNS (Школьный фильтр)
- Yandex.DNS (Семейный режим)
- Собственные DNS-чёрные списки на основе реестра Роскомнадзора

### Управление секретами

- **Нет жёстко закодированных секретов:** Все примеры конфигураций используют заполнители вроде `<ВАШ_ПАРОЛЬ>`
- **Переменные окружения:** Храните чувствительные данные в `.env` файлах (добавьте в `.gitignore`)
- **Контроль доступа:** Ограничьте доступ к репозиторию только для ИТ-персонала

### Соответствие нормативным требованиям

Этот инструментарий учитывает:
- **Федеральный закон 436-ФЗ:** Защита детей от информации, причиняющей вред их здоровью и развитию
- **Приказ 1274:** Требования к сетям образовательных учреждений
- **Рекомендации ФСТЭК:** Базовые меры защиты информации для образовательных организаций

**Юридический дисклеймер:** Предоставленные шаблоны и документы являются **только примерами** и не являются юридической консультацией. Все политики и конфигурации должны быть рассмотрены и утверждены администрацией и юридическим отделом вашего учреждения.

## Структура проекта

```
ru-edu-it-safety-kit/
├── src/
│   └── ru_edu_it_safety_kit/      # Пакет Python
│       ├── cli.py                  # Точка входа CLI (Click)
│       ├── models/                 # Модели данных Pydantic
│       ├── validators/             # Логика валидации профилей
│       └── generators/             # Планировщик мер и генератор листовок
├── configs/
│   ├── topologies/                 # Примеры профилей школ (YAML)
│   ├── openwrt/                    # Шаблоны конфигураций OpenWrt
│   ├── pfsense/                    # Шаблоны конфигураций pfSense
│   ├── routeros/                   # Скрипты MikroTik RouterOS
│   └── logging/                    # Стек централизованного логирования (Docker)
├── policy_templates/               # Markdown-шаблоны для заинтересованных лиц
│   ├── it_rules_for_parents.ru.md
│   ├── it_rules_for_students.ru.md
│   └── it_rules_for_teachers.ru.md
├── tests/                          # Pytest-тесты (unit и integration)
├── docs/                           # Дополнительная документация
├── .github/
│   └── workflows/
│       └── ci.yml                  # CI-пайплайн GitHub Actions
├── pyproject.toml                  # Метаданные проекта Python
├── LICENSE                         # Лицензия Apache-2.0
└── README.ru.md                    # Этот файл
```

## Планы развития

**Версия 0.2.0 (2 квартал 2025):**
- Интерактивный конструктор профилей (`ru-safety init`)
- Экспорт плана безопасности в PDF
- Поддержка конфигураций с резервированием двух провайдеров
- Ansible-плейбуки для автоматизированной настройки устройств

**Версия 0.3.0 (3 квартал 2025):**
- Веб-интерфейс для управления профилями
- Готовые дашборды Grafana для мониторинга сети
- Интеграция с российскими DPI-решениями (соответствие ТСПУ)

**Запросы сообщества:**
- Поддержка шаблонов Cisco IOS
- Интеграция с российскими SIEM-системами
- Многоязычная поддержка (татарский, башкирский, другие региональные языки)

Подробные обсуждения см. в [GitHub Issues](https://github.com/ranas-mukminov/ru-edu-it-safety-kit/issues).

## Как внести вклад

Вклад приветствуется! Этот проект является open-source и ориентирован на сообщество.

### Как помочь

1. **Сообщить о проблеме:** Используйте [GitHub Issues](https://github.com/ranas-mukminov/ru-edu-it-safety-kit/issues) для сообщений об ошибках или запросов функций
2. **Отправить pull request:**
   - Сделайте форк репозитория
   - Создайте ветку для функции: `git checkout -b feature/my-improvement`
   - Внесите изменения
   - Запустите тесты: `pytest`
   - Запустите линтеры: `ruff check . && black --check .`
   - Отправьте PR с чётким описанием

### Рекомендации по стилю кода

- **Python:** Следуйте PEP 8, используйте type hints, максимальная длина строки 120
- **YAML:** Отступы 2 пробела, используйте safe_load
- **Markdown:** Используйте заголовки, списки и блоки кода для читаемости
- **Shell-скрипты:** Используйте `shellcheck` для валидации

### Требования к тестированию

Все новые функции должны включать:
- Unit-тесты для логики (`tests/unit/`)
- Integration-тесты для CLI-команд (`tests/integration/`)
- Примеры конфигураций, демонстрирующие функцию

Запустите полный набор тестов перед отправкой:
```bash
pytest --cov=src/ru_edu_it_safety_kit tests/
```

## Лицензия

Этот проект распространяется под **лицензией Apache 2.0**. Полный текст см. в файле [LICENSE](LICENSE).

Лицензия Apache 2.0 разрешает:
- ✅ Коммерческое использование
- ✅ Модификацию
- ✅ Распространение
- ✅ Использование патентов
- ✅ Частное использование

**© 2025 run-as-daemon.ru**

## Автор и коммерческая поддержка

**Автор:** [Ранас Мукминов](https://github.com/ranas-mukminov)  
**GitHub:** [@ranas-mukminov](https://github.com/ranas-mukminov)

Это open-source проект, поддерживаемый компанией **run-as-daemon.ru** — консалтинговой компанией DevOps и автоматизации инфраструктуры, специализирующейся на российских корпоративных и образовательных ИТ-системах.

### Коммерческие услуги

Для внедрения в продакшене, настройки под ваши нужды и постоянной поддержки мы предлагаем:

- **Аудит школьной ИТ-инфраструктуры:** Оценка текущего состояния безопасности и предоставление детальных планов улучшения
- **Настройка и конфигурация под ключ:** Полное внедрение защищённых школьных сетей с сегментацией VLAN, фильтрацией контента и централизованным мониторингом
- **Обучение и передача знаний:** Практические семинары для ИТ-администраторов и учителей
- **Постоянная поддержка:** Обслуживание, реагирование на инциденты и консультации по соответствию требованиям
- **Разработка под заказ:** Специализированные функции, интеграции с существующими школьными системами (1С, ERP, LMS)

**Контакты:** Посетите [https://run-as-daemon.ru](https://run-as-daemon.ru) или свяжитесь через [GitHub-профиль](https://github.com/ranas-mukminov).

---

**Поддержите проект:** Если этот инструментарий помог вашему учреждению, пожалуйста, поставьте ⭐ звезду репозиторию и поделитесь им с другими школами!
